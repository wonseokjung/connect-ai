# 🗂️ Connect AI — 개선 백로그

> 이슈·개선안을 여기 모아두고, **다음 빌드 때 batch로 반영**한다.
> 상태: 🆕신규 · 🔬진단중 · 🛠️수정대기 · ✅완료(배포) · ⏸️보류
> 우선순위: P0(회원이 막힘) · P1(중요) · P2(개선)

---

## 🔁 루프를 닫다 — 측정·학습·결재 (2026-07-04, v0.5.9 배포)

> v0.5.8에서 미뤘던 P1 2건 + P2 1건을 전부 구현. 적대적 리뷰 3회(무크래시 확인·실결함 각각 수정) + 시뮬 62/62 + 앱시뮬 172/172.

- ✅ **📊 측정 스코어보드**: 사이클 시작마다 매출·구독·조회·커밋 스냅샷 → 다음 사이클에 '지난 이후 +485 USD·+40 구독' delta를 UI(투두·완료)+AI 플랜 프롬프트에. 👍👎(감상) 아니라 실제 지표 변화(결과)로 강화학습 루프 완성. (리뷰 수정: 통화 바뀌면 delta 대신 절대액)
- ✅ **🎓 운영→학습 플라이휠**: 👍한 AI 산출물 → 그 분야 검증 두뇌로 적재, 30개 차면 '🌱 학습하러 가기' 유도. '운영할수록 내 AI가 똑똑해진다' 소유 루프. (리뷰 수정: 중복 가드를 제목+사이클 스코프로 — 다음 사이클 같은 제목=새 예제 정상 누적)
- ✅ **🛡️ 결재함**: 발송·발행·배포·유튜브·명령 결재를 운영 화면 인라인으로 승인/수정/반려. 수정=AI 초안 재생성(앱 내). (리뷰 수정 P1: 결재함을 cycleBody 밖 지속 노드로 + 포커스·세트 가드 → 실행 중 재렌더가 수정 입력 지우던 것 해결)

### 남김 (다음)
- [P2] 성장 사이클 3섹션 UI(구버전) dead code 정리 vs 재연결 — 제품의 심장이라 사장님 결정 필요
- [P2] 윈도우 코드서명(연 $200~400) — 백신 차단·경고 제거
- [P3] 결재 양쪽 화면(관리 패널 ↔ 운영 결재함) 실시간 동기화(현재 각자 새로고침 시 반영)

## ✅ 운영 = 사람–AI 협업 투두리스트 (2026-07-03, v0.5.8 배포)

> "운영 시작"을 실제로 돌아가는 협업 루프로 재구현. 적대적 리뷰 2팀 + 시뮬 47 + 앱시뮬 172 통과 후 배포.

### 배포됨 (v0.5.8)
- ✅ **운영 = 오늘의 투두리스트**: 작전을 위에서부터 하나씩만 해결(순서=파이프라인). 🤖 AI 몫은 ▶ 시키기, 🙋 사장님 몫은 하고 한 줄 보고.
- ✅ **협업 루프 닫힘**: 사장님 보고 → 두뇌 저장 → 다음 사이클 계획이 이어받음(humanReports). 프롬프트에 "어제 결과 위에 쌓기, 매일 리셋 금지" 지시.
- ✅ **퀘스트 로그 레일 UI**: 세로 레일이 진행 따라 차오름, 지금 할 일만 미션 카드. 텍스트 다이어트 + 반짝임 절제(정적·저채도).
- ✅ **내 자산 전부 연결**: 투두·홈 4기둥 → 내 서비스·유튜브 채널·깃허브 저장소·매출 대시보드·두뇌·연동으로 바로가기 칩.
- ✅ **서비스별 깃허브 관리**: get_github(repo)로 서비스별 커밋 조회 + 개발자 지시에 read_repo_file→edit_repo_file(결재→실배포) 절차.
- ✅ **동시성 레이스 3건**(적대적 리뷰 발견): opsInFlight 래치(실행 중 새 사이클 플랜 덮어쓰기·stop 후 이중 실행 차단) + 중복 제목 dedupe + ▶ 담당 코어싱.

### 남김 (다음 배치 — 루프를 '측정'으로 닫기, 새 서브시스템이라 검증 후)
- [P1] **측정 스코어보드**: 사이클 시작 시 "지난 작전 이후 매출·구독·조회 변화" 한 줄 비교. 지금은 👍👎(사람 감상)만 있고 결과 측정이 없어 강화학습 루프가 절반만 닫힘. → 이게 있어야 "운영"이 진짜.
- [P1] **운영→학습 플라이휠**: 결재(승인/수정/반려)를 SFT/DPO 데이터로 자동 적재 → 분야 30개 차면 "학습시키러 가기". 백엔드는 이미 완성(회원 HF 무료), 연결만. "운영할수록 내 AI가 똑똑해진다" = 우리만 가능한 소유 플라이휠.
- [P2] **결재함 UI**: 지금은 request_approval→텔레그램만. 앱 내 결재 대기 인박스(승인/수정/반려) 화면.

## 🛡️ 전체 프로젝트 적대적 점검 (2026-07-01, v0.5.6 대기 — 코드 반영·미배포)

> 병렬 버그헌터 3팀(엔진/메인IPC/렌더러) + 시뮬 172 + 파이썬 문법검증. **확정 버그 20건 수정.**

### P0 (회원이 막힘)
- ✅ **엔진 영구멈춤**: 로딩 중 끄기(⏹/삭제) → `_loading`이 영원히 true → 앱 재시작 전까지 모델 못 켬. stopLocalEngine에서 리셋.
- ✅ **💻 로컬학습 무조건 크래시**: trl 1.7이 `SFTConfig(max_seq_length)` 제거 → 10분 pip 후 사망. inspect로 max_length/max_seq_length 자동. + `q4_k_m`은 convert 스크립트가 거부 → q8_0 매핑. + MPS fp16 NaN 위험 → fp32.

### P1
- ✅ **학습 프로세스 조용한 죽음**: train:local이 공유 터미널 슬롯(termProc) 사용 → 터미널 명령·서버실행·에이전트가 몇시간 학습을 kill. 전용 슬롯(_trainLocalProc) 분리.
- ✅ **학습/진화 상태 충돌**: cloudJob 공유 → 학습 중 진화 시작하면 진행표시 뒤섞여 엉뚱한 모델 설치. 서로 진행 중이면 차단(30분 창).
- ✅ **와이파이 깜빡임에 로그인 쫓겨남**: _authNetErr 전역 플래그를 상태폴링이 리셋하는 레이스 → fbIdTokenEx 호출별 반환으로.
- ✅ **백엔드 타임아웃 → 이중 실행**: 60s 타임아웃 후 직접모드 폴백이 잡 2개·카운트 2번·회원 계정 과금. 폴백 제거, 재시도 안내.
- ✅ **임베딩 모델을 채팅에 오선택**: LM Studio에 임베딩만 있으면 그걸 채팅 모델로 → 전 대화 사망. 채팅가능 모델만 + 다음 엔진 탐색.
- ✅ **DPO 노트북 크래시**: trl 0.16+ `tokenizer=` 제거 → processing_class 자동 심.
- ✅ **다운로드 무결성**: CDN 끊김 → 잘린 .gguf가 정상모델로 등록(재다운 불가) + 진행 UI 영구 행업. 크기검증+aborted 처리+.part 정리.
- ✅ **연결확인 가짜성공**: 실패 안내문에 'LM Studio' 단어가 있어 정규식이 항상 ✅ → '현재 엔진:' 접두로만 판정.
- ✅ **⛔무거움 모델 카드 클릭 구멍**: 버튼만 disabled, 카드 클릭은 다운로드됨 → 카드도 차단.

### P2
- ✅ precheckMerge: pytorch_model.bin(옛 모델) 허용(서버와 기준 일치)·adapter AND 조건·병렬화(24s→12s)
- ✅ cloud:cancel 정직화(서버 잡은 원격취소 불가 안내) / 이름 저장은 성공시만 / recordCreatedModel에 실제 base / Python 3.10+ 검증
- ✅ 한글경로: 새 다운로드는 영문경로, 기존 한글경로 모델도 목록 병합(사라져 보이지 않게)
- ✅ embed 실패 5분 캐시(영구 죽음 방지·base별) / 이어쓰기+평탄화 충돌 / use클릭 레이스(config-실행 모델 불일치)
- ✅ IME 가드(채팅·광장주제) / #localStatus 복원(실패는 시끄럽게) / 서비스 URL escAttr+scheme 차단
- ✅ 🎯 추천두뇌 RAM 맞춤 배지(내 PC에 딱/무거움+차단) — 도감 컨셉 인앱

### 남김 (다음에)
- [P2 · 🛠️] 성장 사이클 3섹션 UI(아이디어/리포트/마케팅)가 buildOpsLoop로 대체되며 미접근 — 의도면 dead code 정리, 아니면 재연결 (제품의 심장이라 사장님 결정 필요)
- [P2] 윈도우 코드서명(연 $200~400) — 백신 차단·경고 제거

---


## 🛡️ 적대적 시뮬레이션으로 발견·수정 (2026-06-20, v0.4.7)

> 병렬 검증 에이전트 2개 + 시뮬레이션으로 v0.4.6 수정본을 재검증 → **실제 버그 발견**.

### [P0 · ✅ v0.4.7] v0.4.6의 문맥절삭이 '스트리밍 채팅'에선 안 먹음 (herrykim 실제 경로!)
- **버그**: 메인 채팅은 스트리밍(`onToken`→streamSSE). 스트림 요청의 400 에러 본문은 **스트림 객체**라 `ctxOverflow()`가 메시지를 못 읽음 → 절삭 미발동. v0.4.6 헤드라인 수정이 정작 herrykim 경로에서 무력.
- **수정**: `bufferStreamError()` — 스트림 에러 본문을 문자열로 읽어 붙임(streamSSE·streamNdjson). 이제 스트리밍에서도 절삭 동작.
- **추가**: `ctxOverflow` 변형 문구도 인식(정규식 보강), `trimForCtx` user없는 리스트도 빈배열 안 만듦.

### [P1 · ✅ v0.4.7] killOrphans가 '클라이언트 연결'까지 죽일 수 있었음
- **버그**: `lsof -ti tcp:1235`는 1235에 **연결한** 프로세스(우리 Electron 앱!)도 잡음 → 자기 앱 kill 위험.
- **수정**: `-sTCP:LISTEN` 추가(리스너만), pid를 killProc 전에 캡처해 자기 보호.

### [P0~P1 · ✅ v0.4.7(코드)] 백엔드 app.py — Space 재업로드 필요
- **#7 개인데이터 유출/소유 상실**: 회원이 HF토큰 줬는데 `hf_whoami` 일시실패 시 **조용히 제공자 계정으로** 떨어뜨려 회원 private 데이터를 제공자 계정에 업로드. → `resolve_owner()`로 **명확히 거부**(조용한 폴백 금지).
- **#5 None게이트 500**: 손상된 게이트(`train:null`)에 `.get().get()` 크래시 → `used_of()` 안전추출.
- **#9 합성 영상 직결**: 공용 스크립트 repo가 기존 private면 회원 토큰 잡이 **마운트 실패** → `update_repo_visibility`로 public 강제.
- **#4 학습모델 덮어쓰기**: 매 학습이 `cai-model` 고정명 덮어씀 → run id(타임스탬프) 붙여 보존.
- **#6 reserve를 try 안으로** — 쓰기 실패해도 500 대신 친절 에러.
- 🧪 `test_logic.py`(스텁 import) 11개 + sim 회귀로 검증.
- ✅ **#2/#1/#3 추가 수정(app.py, 재업로드 대기)**:
  - 💰 **전역 월 잡 하드캡** `GLOBAL_MONTHLY`(기본 60) — userId를 위조·로테이션해도 못 넘는 비용 상한. FIREBASE_API_KEY 미설정 시 캡 우회(#2)의 실질 방어. 잡 직전 선점+롤백(gres 플래그로 정확).
  - 🛡️ **전역키 충돌 가드** — 악성 `userId="__global__"`이 전역 카운터 조작 못 하게 차단.
  - 🆔 **#3 합성 결과 덮어쓰기 방지** — merge out_repo에도 run id(타임스탬프) 부여(학습과 일치).
  - 🧪 `test_logic.py` 16개(전역캡 포함)로 검증.
- ⚠️ **여전히 운영 권고**: `FIREBASE_API_KEY` 설정하면 per-user 캡까지 견고(스푸핑 차단). 미설정이어도 이제 전역캡이 비용을 막음.

---

## 🔴 회원 제보 (Member-reported)

### [P0 · 🔬진단중] 윈도우에서 "모델을 못 켜고 있습니다"
- **제보**: JohnLee — Win11 3대(RTX 3060 12GB/64G, RTX 2060/128G, i7 노트북)에서 모두 모델 실행 실패. 사양 충분, 남들은 더 낮은 사양에서도 정상 → **이 회원 환경 특유 문제**.
- **추정 원인(순서)**:
  1. 🛡️ 백신/Defender가 무서명 `llama-server.exe` 차단
  2. 🇰🇷 한글 Windows 사용자명 → 모델 경로(`C:\Users\한글\...`)를 llama.cpp가 못 엶
  3. 📦 VC++ 재배포 누락 (code 0xC0000135)
- **확인**: 회원에게 화면 **빨간 에러 전체("code …" 포함)** 받기 → 원인 특정
- **근본 수정안(다음 빌드)**:
  - [ ] 윈도우 **코드서명**(백신 차단 원천 제거) — 연 $200~400
  - [ ] 모델을 **자동으로 영문 경로**에 저장(한글 경로 회피) 또는 한글경로 감지 시 경고/우회
  - [ ] 첫 실행 시 VC++ 런타임 체크/안내

---

## 🟠 코드 리뷰 — 미배포 (이번 세션 발견, v0.4.5 대기)

### 백엔드 (app.py) — Space 재업로드로 반영
- ✅ 학습 월3회 통일 / repo 회원별 격리 / 캡 선점·롤백 (배포됨)
- [P1 · 🛠️] idToken 검증 허점 수정본(키 설정 시 토큰 없으면 거부) — `FIREBASE_API_KEY` 넣을 때만 의미. 갱신 app.py 재업로드 필요
- [P2] CORS 전체개방 / provider 캐시 staleness / 에러 원문 노출

### 데스크톱 (v0.4.5 빌드 필요)
- [P1 · 🛠️] 합성 `gated`(월3회)·`needLogin` 처리(로그인창) + 결제오해 제거
- [P1 · 🛠️] 미로그인+본인토큰 시 통과(학습과 일치)
- [P1 · 🛠️] 합성 GGUF 미생성 메시지 합성용 분기 + 설치창 결과링크·재시도·🤖내AI열기
- [P2 · 🛠️] 🔒 비번칸 안내 / 🆓 Colab "무료 HF계정 필요"+토큰링크 / 월0회 시 🆓 유도

> ⚠️ 위 데스크톱 수정은 **코드엔 반영·커밋됨**, v0.4.5 빌드해야 사용자에게 나감.

---

## 🟡 투자/개선 아이디어

- [P1] **윈도우 코드서명 인증서** (연 $200~400) — 첫 실행 경고 제거 = 윈도우 이탈률↓ (100만 목표 ROI 1위)
- [P2] **HF Jobs 예산 상향** ($20 → $50~100/월) — 회원 늘어 "월 예산 소진" 잦아지면. *지금은 보류*
- [P2] **HF Space 항상 켜두기** (~$9~22/월) — 콜드스타트 30초 제거
- [P3] 더 큰 모델(7B+) 지원 — GPU 업그레이드 필요, 교육 가치

---

## ✅ 완료 (배포됨 — 참고)
- **v0.4.5 (2026-06-19 배포)**: 🎓 데이터셋·모델 회원 HF 계정 소유(하이브리드)·🪟 윈도우 GPU(vulkan)+크래시폴백·🎬 인앱 진행카드(애니+라이브로그)·🆓 무료서버 우선·문구 전문화(학습강도·강도λ)·타임아웃 600s. 릴리즈 15종 공개 + ezerai.xyz·connectai-desktop.web.app 둘 다 라이브
- v0.4.4: 합성·학습 백엔드 HF Space 이전(GCP 탈출), 회원 무료 HF Job 제공, GGUF 자동변환, 무료Colab+비번서버 2경로, 사무실·SEO/GEO·각인·UI정리
- CI 릴리즈 직접첨부(아티팩트 quota 우회) + contents:write 권한

---

## 🔴 회원 제보 추가 (2026-06-20)

### [P1 · ✅ v0.4.6] 앱 2벌 동시 실행 → 포트 1235 충돌 → "불러오는중" 영원 / code 1
- **발견**(사장님 맥 테스트): 설치본 + 새 빌드가 동시에 떠서 둘 다 llama-server를 1235에 띄우려다 충돌
- **수정(v0.4.6)**: ① main.ts **`app.requestSingleInstanceLock()`** — 두 번째 인스턴스 즉시 종료 + 기존 창 포커스(`second-instance`) ② localengine **`killOrphans()`** — 시작 전 포트 1235를 문 좀비 llama-server(우리 _proc 제외) lsof/netstat로 회수. 맥 OOM(code 1)은 기존 GPU→CPU→작은ctx 폴백이 이미 커버
- **회원 임시 안내**(구버전): 앱을 ⌘Q로 완전히 끄고 하나만 실행

### [P2 · ✅ v0.4.6(부분)] 깨진 저양자화(IQ2) 빈 출력 + 유령 모델 목록
- **발견**: `gemma-4-E2B-it-UD-IQ2_M`(2비트)는 로드돼도 **빈 출력(finish=length, content='')** → "< 한 토큰 EOS". UI "내 모델"이 삭제된 파일을 stale로 표시
- **수정(v0.4.6)**: 빈 출력 진단 메시지에 **① 손상·미완성 다운로드 ② 과도양자화(IQ2) ③ 템플릿 불일치** 원인 + "Q4_K_M 표준모델로·다시 받기" 안내 추가
- **남음(다음 빌드)**: 유령 목록은 렌더러가 파일 변경 후 재조회 안 하는 것(listLocalModels 자체는 매번 새 스캔 — 정상). 삭제/다운로드 후 자동 새로고침 필요(P3)


### [P0 · ✅ v0.4.6] 채팅이 "context size 초과(HTTP 400)"로 막힘
- **제보**(herrykim): 채팅창에 쓰기만 하면 `HTTP 400: request (8386 tokens) exceeds the available context size (8192 tokens)` → 무엇을 써도 에러
- **원인**: llm.ts에 히스토리 자동 트리밍이 없어 시스템 프롬프트+주입지식+누적 대화가 ctx 초과 시 llama-server 400
- **수정(v0.4.6)**: llm.ts `ctxOverflow()`로 에러의 실제 토큰수 파싱 → `trimForCtx()`가 **이번 요청의 토큰/문자 실측 비율**로 예산을 잡아 **오래된 히스토리부터 절삭**(system·마지막 user는 유지), 그래도 넘치면 거대 system 내용까지 잘라 맞춤 → **에러 대신 자동 재시도**. `callOnce`·`completeWithTools` 양 경로 적용. sim 회귀테스트 7개 추가
- **회원 임시 안내**(구버전): 새 대화 시작 / 설정 '문맥 길이' 16K

---

## 🔬 v0.4.5 배포 후 검증 필요 (실기기/실계정)

### [P0 · 🔬 미검증] 회원 HF 계정 소유 경로 (app.py 하이브리드)
- **변경**: 회원 HF 연동 시 → 학습 데이터셋·결과 모델이 **회원 계정**에 저장. 잡은 **회원 토큰으로 HF I/O**, GPU launch만 제공자. 미연동/구버전 = 제공자 계정 폴백(무중단)
- **미검증 핵심**: HF Jobs가 "런처(제공자)≠잡 HF_TOKEN(회원)"을 허용하고 컴퓨트는 제공자에 과금하는지, 공개 스크립트 dataset 마운트가 회원 토큰 잡에서 되는지 → **로컬에서 검증 불가(회원 토큰 없음)**
- **확인 절차**: 새 app.py 재업로드 → 사장님 HF 연동 상태로 **합성 1번** → 결과가 **사장님 HF 계정**에 뜨면 성공. 안 뜨면 폴백은 안전하므로 무중단, 코드만 수정
- ⚠️ **app.py는 아직 Space에 재업로드 안 됨** — 현재 Space는 이전(모델만 회원, 데이터는 제공자) 버전

### [P1 · 🔬 미검증] 윈도우 GPU(vulkan) 빌드 — 실제 GPU PC 테스트
- vulkan 빌드 동봉 + 크래시(0xC0000005) 시 cpu 폴백 코드 들어감. **실제 윈도우 NVIDIA PC에서 미검증** → redpooh87 등 GPU 회원과 베타 확인 권장

### [P2] UX: 데이터 업로드 단계가 무료(Colab)용임을 명시
- 장기학습 화면 "🤗 HF 데이터셋 업로드" 단계가 유료 회원에게 "내가 올려야 하나?" 오해 유발. "무료(Colab)용 · 유료 서버는 자동" 라벨 추가

---

## 🔴 회원 제보 추가 (2026-06-19)

### [P0 · 🛠️] 장기기억 학습이 "HF Pro 필요"로 막힘
- **제보**: 회원이 장기 학습 누르면 `⚠ HF Jobs는 HF Pro($9/월) 필요…` + hf CLI 명령
- **원인**: train:cloud가 무료 백엔드(우리 서버)를 못 타고 **직접 모드(본인 HF토큰)**로 폴백 → 403 Pro벽. 회원이 ①로그인 안 함 ②비번 없음 ③본인 HF토큰만 있음 중 하나
- **수정안(v0.4.5)**: 학습 needsPro에서도 **🆓 무료(Colab)로 유도** (합성 surgFail처럼). renderer cloud 학습 결과 핸들러(~2465)에 무료 버튼/안내. 회원이면 로그인+비번 시 우리 서버로 자동
- **회원 임시 안내**: 🆓 무료로 시작(코랩) 쓰거나, 멤버면 로그인+베타비번 → 우리 서버 무료

### [P1 · 🛠️] 저사양 PC 응답 3분 타임아웃
- **제보**(빨개지는딸기): 질문 3분 뒤 `timeout of 180000ms exceeded`
- **원인**: llm.ts 로컬 추론 POST timeout=180000(3분). 저사양 CPU+큰 모델은 첫 토큰이 3분 초과
- **수정안(v0.4.5)**: ① timeout 상향(예: 600s) ② 스트리밍 경로 우선(토큰 흐르면 타임아웃 안 침) ③ "아직 생성 중…" 진행 표시로 실패처럼 안 보이게. llm.ts:177/186/244/255
- **회원 임시 안내**: 더 작은 모델(Gemma 4 **E2B**)로 바꾸면 빨라짐

### [P0 · 🛠️ 정정] 회원 본인 HF 토큰이 있으면 무료 서버를 안 탐 (결제벽)
- **진짜 원인**: 옛 레슨에서 "연동에 본인 HF 토큰 넣으세요" 안내 → 그 회원들은 자기 토큰 보유. train:cloud/surgery:merge가 **로그인 안 한 상태 + 본인 토큰 있으면 무료 백엔드를 건너뛰고 직접모드(본인 토큰)로** → Pro벽
- **핵심 수정**: 무료 백엔드(우리 서버)를 **항상 우선** 타게. 본인 토큰은 백엔드 실패 시에만 폴백. 즉 `if (!fbApiKey() || user)` 게이트가 본인토큰 보유 시 백엔드를 스킵하는 로직 수정(main.ts train:cloud ~1875, surgery ~1963). 로그인 유도도 함께
- 영향: 옛 영상 따라 토큰 넣은 회원 다수가 이걸로 막혀 있을 것 → P0

### [P0 · 🔬 핵심] 윈도우가 GPU(VRAM)를 안 씀 → CPU로만 → 느림/타임아웃
- **제보**(redpooh87): 작업관리자 확인 결과 VRAM 미사용, CPU 연산 → 매우 느림. CUDA 연결 방법 문의
- **진짜 원인**: 윈도우 동봉 빌드가 **`cpu` 변종**(vendor/fetch-llamacpp.mjs:19 `WIN_VARIANT='cpu'`). 구형 CPU 크래시(0xC0000005) 막으려 일부러 CPU전용 → **NVIDIA 있어도 GPU 안 씀**. 모든 윈도우 사용자가 CPU 추론 = 느림(타임아웃 다발)
- **근본 수정안**: win **vulkan 빌드(GPU)** 동봉 + 런타임에 GPU 먼저 시도, 구형 CPU 크래시(0xC0000005) 시 cpu 빌드로 폴백(이중 바이너리). fetch-llamacpp(둘 다 받기)·localengine(binPath GPU우선+크래시 폴백)·CI 변경
- **⚠️ 리스크**: 윈도우 전용 변경인데 **여기(맥)서 테스트 불가** → GPU 회원(redpooh87 등) 베타 테스트 후 배포 권장. 잘못하면 윈도우 전체 회귀 위험
- **임시**: 타임아웃 180→600초 상향(완료) + 작은 모델(E2B) 안내

### [P1 · 🛠️] 학습 "진행상황 보기" 링크가 제공자 HF 작업이라 회원에게 로그인 요구
- **증상**: 무료 서버 학습 시작 후 "진행상황 보기" 클릭 → 제공자(사장님) HF 작업/모델 페이지 → HF 로그인 요구. 회원은 볼 수 없음
- **수정안(v0.4.5)**: 백엔드(viaBackend) 작업은 이 링크 제거하거나 앱 내부 진행률(cloudPoll)만 표시. renderer ~2449 `r.url||r.modelRepo` 링크를 viaBackend면 숨김. 합성 surgRunning(~2712)도 동일 검토
